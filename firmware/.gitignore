# Firmware secrets — copy config.example.h to config.h and fill in locally.
# config.h holds Wi-Fi creds (and the LLM key from v0.2); never commit it.
config.h

# PlatformIO build output
.pio/

# Arduino / arduino-cli build output
build/
*.bin
*.elf
*.hex
*.map

# Host test binaries are built by `pio test -e native` into .pio/ (ignored above).
