# Pyramid server runtime + test dependencies (v2.1).
# Pinned loosely; CI installs these to run lint + pytest.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
websockets>=12.0
httpx>=0.27          # async HTTP for the real ASR/LLM/TTS adapters + TestClient
pytest>=8.0
pytest-asyncio>=0.23
ruff>=0.6            # lint
